%p Are you sure you want to delete "#{@post.title}"?

%form(action="/posts/#{@post.id}" method="post")
  %input(type="hidden" name="_method" value="delete")
  %input(type="submit" value="Delete")
  %a(href="/posts/#{@post.id}/edit") Cancel